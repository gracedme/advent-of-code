const one = require('./one');
const two = require('./two');

const actualInput = ``;

describe("day ", () => {
  describe("part 1", () => {
    test("actual input", () => {
      // expect(one(actualInput)).toBe();
    })
  });

  describe("part 2", () => {
    test("actual input", () => {
      // expect(two(actualInput)).toBe();
    })
  })
});
