const one = require('./one');
const two = require('./two');
const ACTUAL_INPUT = require('./input');


describe("day ", () => {
  const testInput = ``;
  describe("part 1", () => {
    test("test input", () => {
      // expect(one(testInput)).toBe();
    })

    test("actual input", () => {
      // expect(one(ACTUAL_INPUT)).toBe();
    })
  });

  describe("part 2", () => {
    test("test input", () => {
      // expect(two(testInput)).toBe();
    })

    test("actual input", () => {
      // expect(two(ACTUAL_INPUT)).toBe();
    })
  })
});
